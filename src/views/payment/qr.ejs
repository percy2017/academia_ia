<%- contentFor('title') %>
Pago con QR - Academia AI
<%- contentFor('body') %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card text-center shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Completa tu Pago con QR</h4>
                </div>
                <div class="card-body">
                    <p class="lead">Plan: <strong><%= plan.name %></strong></p>
                    <h2 class="display-5 fw-bold text-success mb-3"><%= amountInBob %> Bs.</h2>
                    
                    <p>Escanea el siguiente código QR con tu aplicación bancaria para realizar el pago.</p>
                    
                    <div class="my-4">
                        <img src="<%= qrLink %>" alt="Código QR para el pago" class="img-fluid rounded" style="max-width: 300px;">
                    </div>

                    <hr>

                    <h5 class="mt-4">Sube tu Comprobante</h5>
                    <p class="text-muted">Una vez realizado el pago, sube una captura de pantalla o foto del comprobante para que podamos verificar tu suscripción.</p>
                    
                    <form action="/payment/qr/upload-receipt" method="POST" enctype="multipart/form-data">
                        <input type="hidden" name="subscriptionId" value="<%= subscriptionId %>">
                        <div class="mb-3">
                            <input class="form-control" type="file" id="receipt" name="receipt" required>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-cloud-arrow-up-fill me-2"></i>He Realizado el Pago
                            </button>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-muted">
                    Tu suscripción se activará una vez que nuestro equipo verifique el pago.
                </div>
            </div>
        </div>
    </div>
</div>
